Скрипт созданный Евгением Македоном.

Раз  в  сутки примерно в 3 часа утра, все логи (только voice calls) за предыдущий день пересылаются в минск в папку (\\Srv-logsblr-001\CoverageLogs\) на сервер Srv-logsblr-001.

Имя лог файлов формируется: следующим образом (в <> указаны поля таблицы, в австрийской БД): <agent>_<tea_logid>_<mtime>.log
Где mtime форматируется как yyyy-MM-dd_hh-mm-ss
 
